services:
  quartz:
    container_name: quartz
    build: .
    expose:
      - "8080"
    volumes:
      - ${VAULT_PATH}:/usr/src/app/obsidian-vault
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    external: true
